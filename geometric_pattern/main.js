var NUM_CIRCLES = 12;
var circle Diameter;

function setup() {
    createCanvas(480, 600);
    circleDiameter = width/NUM_CIRCLES;
}

function draw() {
    var x = 0
    while (x, 0, circleDiameter, circleDiameter);
    x = x + circleDiameter
  }
  
    y = y + circleDiameter
  }    
}